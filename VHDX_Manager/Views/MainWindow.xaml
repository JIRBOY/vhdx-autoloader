<Window x:Class="VHDX_Manager.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:VHDX_Manager.Converters"
        mc:Ignorable="d"
        Title="VHDX Manager" Height="600" Width="900"
        Icon="pack://application:,,,/Resources/main.png"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- 转换器 -->
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <converters:BoolToMountStatusConverter x:Key="BoolToMountStatusConverter"/>
        <converters:BoolToAutoMountStatusConverter x:Key="BoolToAutoMountStatusConverter"/>
        <converters:MountPreferenceConverter x:Key="MountPreferenceConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToServiceButtonConverter x:Key="BoolToServiceButtonConverter"/>
        <converters:BoolToStatusColorConverter x:Key="BoolToStatusColorConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- 按钮样式 -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="BorderBrush" Value="#BDBDBD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#EEEEEE"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D32F2F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#C62828"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="BorderBrush" Value="#BDBDBD"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="Foreground" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid Margin="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="VHDX Manager" FontSize="18" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="+ 挂载新磁盘" Style="{StaticResource PrimaryButton}"
                            Command="{Binding MountCommand}" Margin="0,0,8,0"/>
                    <Button Content="&#xE72C; 刷新" Style="{StaticResource SecondaryButton}"
                            Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                    <Button Style="{StaticResource SecondaryButton}"
                            Command="{Binding InstallServiceCommand}"
                            Visibility="{Binding IsServiceInstalled, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,4,0"/>
                            <TextBlock Text="安装服务"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource DangerButton}"
                            Command="{Binding UninstallServiceCommand}"
                            Visibility="{Binding IsServiceInstalled, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74C;" FontFamily="Segoe MDL2 Assets" Margin="0,0,4,0"/>
                            <TextBlock Text="卸载服务"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 降级模式提示栏 -->
        <Border Grid.Row="1" Background="#FFF3E0" BorderBrush="#FFE0B2" BorderThickness="0,0,0,1"
                Visibility="{Binding IsDegradedMode, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid Margin="16,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#E65100" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding DegradedModeMessage}" Foreground="#E65100" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 虚拟磁盘列表 -->
        <Grid Grid.Row="2" Margin="16">
            <ListView ItemsSource="{Binding VirtualDisks}"
                      SelectedItem="{Binding SelectedDisk}"
                      BorderThickness="0"
                      Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FFFFFF" CornerRadius="8" Margin="0,0,0,8" Padding="16"
                                BorderBrush="#E0E0E0" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- 第一行：文件路径和状态 -->
                                <Grid Grid.Row="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="16"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding FilePath}" FontSize="14" FontWeight="SemiBold"
                                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Ellipse Width="8" Height="8" Margin="0,0,6,0"
                                                 Fill="{Binding IsMounted, Converter={StaticResource BoolToStatusColorConverter}}"/>
                                        <TextBlock Text="{Binding IsMounted, Converter={StaticResource BoolToMountStatusConverter}}"
                                                   Foreground="{Binding IsMounted, Converter={StaticResource BoolToStatusColorConverter}}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>

                                <!-- 第二行：详细信息 -->
                                <Grid Grid.Row="1" Margin="0,8,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="挂载点: " Foreground="#757575"/>
                                        <TextBlock Text="{Binding MountPointDisplay}" Margin="0,0,16,0"
                                                   ToolTip="{Binding DriveLetter}"/>
                                        <TextBlock Text="格式: " Foreground="#757575"/>
                                        <TextBlock Text="{Binding Format}" Margin="0,0,16,0"/>
                                        <TextBlock Text="容量: " Foreground="#757575"/>
                                        <TextBlock Text="{Binding TotalSizeText}" Margin="0,0,16,0"/>
                                        <TextBlock Text="可用: " Foreground="#757575"/>
                                        <TextBlock Text="{Binding FreeSpaceText}" Margin="0,0,16,0"/>
                                        <TextBlock Text="挂载: " Foreground="#757575"/>
                                        <TextBlock Text="{Binding MountPreference, Converter={StaticResource MountPreferenceConverter}}"
                                                   Cursor="Hand" Foreground="#2196F3"
                                                   ToolTip="点击切换挂载模式">
                                            <TextBlock.InputBindings>
                                                <MouseBinding MouseAction="LeftClick"
                                                              Command="{Binding DataContext.CycleMountPreferenceCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                              CommandParameter="{Binding}"/>
                                            </TextBlock.InputBindings>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>

                                <!-- 第三行：操作按钮 -->
                                <Grid Grid.Row="2" Margin="0,12,0,0">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <ToggleButton Content="{Binding IsAutoMount, Converter={StaticResource BoolToAutoMountStatusConverter}}"
                                                      IsChecked="{Binding IsAutoMount, Mode=OneWay}"
                                                      Command="{Binding DataContext.ToggleAutoMountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                      CommandParameter="{Binding}"
                                                      Style="{StaticResource ToggleButtonStyle}"
                                                      Padding="12,6" Margin="0,0,8,0"/>
                                        <Button Content="卸载" Style="{StaticResource DangerButton}"
                                                Command="{Binding DataContext.UnmountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding}"
                                                Padding="12,6"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>

            <!-- 空状态提示 -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                        Visibility="{Binding VirtualDisks.Count, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                <TextBlock Text="&#xE7F4;" FontFamily="Segoe MDL2 Assets" FontSize="48" Foreground="#BDBDBD"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="暂无虚拟磁盘" FontSize="16" Foreground="#9E9E9E"
                           HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Text="点击 '挂载新磁盘' 按钮添加虚拟磁盘" FontSize="13" Foreground="#BDBDBD"
                           HorizontalAlignment="Center" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="3" Background="#FFFFFF" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid Margin="16,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding DiskCountText}" Foreground="#757575" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="#E0E0E0" VerticalAlignment="Center" Margin="8,0"/>
                    <TextBlock Text="{Binding LastRefreshTimeText}" Foreground="#757575" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="#E0E0E0" VerticalAlignment="Center" Margin="8,0"/>
                    <TextBlock Text="{Binding ServiceStatusText}"
                               Foreground="{Binding ServiceStatusBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#757575" VerticalAlignment="Center"/>
                    <ProgressBar Width="100" Height="16" Margin="8,0,0,0"
                                 IsIndeterminate="{Binding IsLoading}"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>